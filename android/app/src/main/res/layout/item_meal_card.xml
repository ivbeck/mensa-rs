<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/card_margin_bottom"
    app:cardBackgroundColor="@color/bg_surface"
    app:cardCornerRadius="@dimen/card_radius"
    app:cardElevation="4dp"
    app:cardMaxElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="@dimen/card_min_height"
        android:orientation="vertical"
        android:padding="@dimen/card_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/meal_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/ink"
                android:textSize="@dimen/text_card_title"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/price_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_price_badge"
                android:paddingStart="@dimen/price_badge_padding_horizontal"
                android:paddingEnd="@dimen/price_badge_padding_horizontal"
                android:paddingTop="@dimen/price_badge_padding_vertical"
                android:paddingBottom="@dimen/price_badge_padding_vertical"
                android:textColor="@color/ink_muted"
                android:textSize="@dimen/text_badge" />

        </LinearLayout>

        <TextView
            android:id="@+id/meal_ingredients"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:textColor="@color/ink_muted"
            android:textSize="@dimen/text_card_ingredients" />

    </LinearLayout>

</androidx.cardview.widget.CardView>